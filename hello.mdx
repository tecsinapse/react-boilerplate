---
name: Iniciando
---

import { Playground, PropsTable } from 'docz'
import {Button} from '@tecsinapse/ui-kit'
import ThemeProvider from './src/ThemeProvider'

# Documentação Inicial UI-KIT

Coloque o ThemeProvider na raiz da aplicação, escolhendo a variante.

## ThemeProvider

<PropsTable of={ThemeProvider} />

### Uso Básico

<Playground>
    <ThemeProvider variant="orange">
        <Button submitting onClick={()=>{}}> Teste </Button>
    </ThemeProvider>
</Playground>

